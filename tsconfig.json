import { configure } from 'quasar/wrappers'

export default configure(() => ({
  boot: ['pinia', 'axios'],
  css: ['app.scss'],
  extras: ['material-icons', 'roboto-font'],
  build: {
    target: {
      browser: ['es2022', 'firefox115', 'chrome115', 'safari14'],
      node: 'node18'
    },
    typescript: {
      strict: true,
      vueShim: true
    },
    env: {
      API_BASE_URL: process.env.VITE_API_BASE_URL || 'http://localhost:3000',
      WS_URL: process.env.VITE_WS_URL || 'ws://localhost:3000',
      BUILD_ENV: process.env.VITE_BUILD_ENV || 'dev',
      APP_VERSION: process.env.VITE_APP_VERSION || '0.1.0'
    },
    vueRouterMode: 'history'
  },
  devServer: {
    open: false
  },
  framework: {
    config: {},
    plugins: ['Notify', 'Dialog', 'Loading']
  },
  animations: [],
  pwa: {
    workboxMode: 'InjectManifest',
    swFilename: 'sw.js',
    manifest: {
      name: 'Event Platform',
      short_name: 'Events',
      description: 'Кроссплатформенное приложение для управления событиями',
      display: 'standalone',
      orientation: 'portrait',
      background_color: '#f7faf8',
      theme_color: '#1d6f5f',
      icons: [
        {
          src: 'icons/icon-192x192.png',
          sizes: '192x192',
          type: 'image/png'
        },
        {
          src: 'icons/icon-512x512.png',
          sizes: '512x512',
          type: 'image/png'
        }
      ]
    }
  }
}))
